***

# Инструкция по доступу к заданиям


Для получения репозитория для выполнения задания:
- Перейдите по ссылке gitHub classroom из лабораторной
  - при первом входе система предложит вам выбрать себя в списке и привязать свой github аккаунт
    <img width="1192" height="1117" alt="classroom" src="https://github.com/user-attachments/assets/183e6917-b8e1-41e8-8b8c-c61c908f1d00" />
  - при подтверждении, на почту, привязанную к github должно прийти письмо с приглашением
    <img width="1388" height="666" alt="classroom1" src="https://github.com/user-attachments/assets/e307407a-0ff3-4c15-b9a0-8f386ea6d6de" />
    <img width="1584" height="855" alt="image" src="https://github.com/user-attachments/assets/35c0e447-d21c-4fac-9b28-eefb011207c5" />
    <img width="1189" height="1077" alt="classroom3" src="https://github.com/user-attachments/assets/5d7fade2-98ef-41f1-b5ea-fcc68f66de4a" />
- Репозиторий автоматически клонируется в вашу учетную запись
- Создается **Pull Request с названием `feedback`** — **не трогайте его!**

**!Внимание:** у приглашения ограниченный срок жизни. По его окончанию, если вы не успели его принять, потребуется запросить у преподавателя новую сслыку. 

## Клонирование проекта

```bash
git clone <URL_репозитория>
```

**Или в IntelliJ IDEA:**
1. File → New → Project from Version Control
<img width="887" height="234" alt="image" src="https://github.com/user-attachments/assets/f3ca26b9-a5ca-4842-bf80-6732c2bdece1" />

2. Вставьте URL репозитория


## Создание ветки задания

Создайте новую ветку по номеру задания:

```bash
git checkout -b task-1
```

**Или в IDEA:**
- В углу: `main` → `+ New Branch` → `task-1`
<img width="842" height="368" alt="image" src="https://github.com/user-attachments/assets/82badba3-38a9-460e-8398-36e0bd048d73" />

## Выполнение задания

1. Выполните задание строго по инструкции
2. **Обязательно коммитьте изменения** с осмысленными сообщениями:
   ```bash
   git add .
   git commit -m "Реализовано задание 1: добавлена валидация входных данных"
   ```
**Или в IDEA:**
<img width="1095" height="941" alt="image" src="https://github.com/user-attachments/assets/812f50fe-4df3-4b8f-ab24-8106d22fa4ae" />

### Коммит + Push
```bash
git add .
git commit -m "Завершено задание 1"
git push origin task-1
```

## Отправка на проверку

### Создание Pull Request

**В GitHub:**
1. Перейдите в раздел `Pull requests`
2. Нажмите `Compare → changes`
3. Укажите:
    - **Заголовок:** `task-1 Фамилия Имя`
    - **Описание:** краткий комментарий по реализации (по желанию)

**В IDEA:**
- `VCS → Git → Create Pull Request`
- 
**Пример PR описания**

```
task-1 Иванов Иван

Реализация:
- Добавлена валидация входных параметров
- Оптимизирован алгоритм поиска (O(n) → O(log n))
- Добавлены unit-тесты (100% покрытие)
```

## Доработка (если нужны правки)

1. **Внесите изменения** в код (остаетесь в ветке `task-1`)
2. **Сделайте commit + push**:
   ```bash
   git add .
   git commit -m "Исправлены замечания: добавлена обработка edge-кейсов"
   git push origin task-1
   ```
3. **Новый PR создавать НЕ нужно** — старый обновится автоматически!

## Сдача работ

Как только PR попадает в очередь, преподаватель может его проверить. У проверки может быть 3 результата:

1. Преподаватель не удовлетворён работой
   Такой результат происходит когда преподаватель оставляет change request в вашем pull request и ваш PR пропадёт
   из очереди. Вы должны будете исправить замечания и загрузить исправления в ту же ветку, в тот же pull request. 
2. Преподаватель предварительно одобрил работу
   Такой результат происходит когда преподаватель ставит approve вашему pull request. Это значит что преподаватель
   удовлетворён вашим кодом, но ожидает защиты лабораторной. 
3. Преподаватель удовлетворён работой
   Такой результат происходит когда преподаватель выполняет merge на вашем pull request, или использует специальные
   команды для оценки.
 

**Важно! PR оценивается только преподавателям, если вы смержите pull request до того как он будет оценён
преподавателем, он будет просто деактивирован и пропадёт из очереди.**

# Дедлайны и штрафы

Система предоставляет возможность преподавателям настраивать дедлайны по выполнению лабораторных работ. 
За каждую неделю просрочки дедлайнов, ваши максимальные баллы за лабораторную работу сокращаются на 20%.
***
